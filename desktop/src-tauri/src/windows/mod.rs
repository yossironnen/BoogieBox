//! Defines Tauri desktop shell logic for Mod.

/// Windows-specific helpers.

/// Returns true if another instance of the app is already running.
/// Stub — full named-mutex implementation can be added in a later phase.
#[allow(dead_code)]
pub fn is_already_running() -> bool {
    false
}
