# Git Requirements

DO NOT submit any changes to GIT when changing code, unless explicitly requested by the user.

Before submitting changes:

1. Update `README.md` with any changed folder structure, file descriptions, or API calls.
2. Run `npm test`
3. Run `npm run lint`
4. Ensure coverage > 80%
